# 操作系统专题进度

## 已整理

- [x] 用户态、内核态、系统调用、异常和中断
- [x] 进程、线程、协程、进程状态和上下文切换
- [x] CPU 调度算法、锁、死锁和银行家算法
- [x] 进程间通信方式对比与选型
- [x] 虚拟内存、地址翻译、页表、MMU 和 TLB
- [x] `fork` 写时复制、`brk`、`mmap`、缺页异常、内存回收和 OOM
- [x] 页面置换算法：OPT、FIFO、LRU、Clock、LFU、工作集
- [x] I/O 多路复用：select、poll、epoll、LT/ET、Reactor
- [x] 零拷贝：普通 I/O、`mmap`、`sendfile`、DMA

## 本次处理原则

- 只沉淀文字内容和结构化图示，不保存参考素材文件。
- 对 Linux、Redis、Nginx 已经覆盖过的 epoll/Reactor 内容，仅在操作系统视角补充内核机制和面试追问。
- 对基础概念不做简单搬运，重点补“为什么、代价、边界和工程取舍”。

## 后续可继续补充

- Linux CFS 调度器、nice 值、vruntime。
- futex、用户态锁和内核阻塞路径。
- NUMA、透明大页、内存屏障和缓存一致性。
- AIO、io_uring 与传统 epoll 的差异。
- 文件系统缓存、脏页回写和 `fsync` 语义。

